{
  "name": "open-pair",
  "displayName": "Open Pair",
  "description": "VS Code extension for real-time collaborative editing, compatible with live-share.nvim. Host or join sessions with E2E encryption, remote cursors, follow mode, and shared terminal — no Microsoft account required.",
  "version": "0.2.4",
  "publisher": "darkerthanblack2000",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/darkerthanblack2000/open-pair"
  },
  "bugs": {
    "url": "https://github.com/darkerthanblack2000/open-pair/issues"
  },
  "homepage": "https://github.com/darkerthanblack2000/open-pair#readme",
  "icon": "icon.png",
  "galleryBanner": {
    "color": "#1e1e2e",
    "theme": "dark"
  },
  "keywords": [
    "live-share",
    "collaboration",
    "neovim",
    "co-editing",
    "pair-programming",
    "real-time"
  ],
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "openPair.join",
        "title": "Open Pair: Join Session"
      },
      {
        "command": "openPair.startServer",
        "title": "Open Pair: Start Hosting"
      },
      {
        "command": "openPair.stop",
        "title": "Open Pair: Stop Session"
      },
      {
        "command": "openPair.follow",
        "title": "Open Pair: Toggle Follow Mode"
      },
      {
        "command": "openPair.openWorkspace",
        "title": "Open Pair: Open Workspace File"
      },
      {
        "command": "openPair.showPeers",
        "title": "Open Pair: Show Peers"
      },
      {
        "command": "openPair.debugInfo",
        "title": "Open Pair: Debug Info"
      }
    ],
    "configuration": {
      "title": "Open Pair",
      "properties": {
        "openPair.connectTimeout": {
          "type": "number",
          "default": 15,
          "minimum": 1,
          "markdownDescription": "Seconds to wait for the connection to the host to be established before giving up. Raise this on slow tunnels."
        },
        "openPair.approvalTimeout": {
          "type": "number",
          "default": 60,
          "minimum": 1,
          "markdownDescription": "Seconds to wait for the host to accept the join request, once connected. This covers someone answering a dialog, so it is deliberately generous."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run bundle",
    "bundle": "esbuild src/extension.ts --bundle --outfile=out/extension.js --external:vscode --platform=node --target=node18 --minify",
    "compile": "esbuild src/extension.ts --bundle --outfile=out/extension.js --external:vscode --platform=node --target=node18 --sourcemap",
    "watch": "esbuild src/extension.ts --bundle --outfile=out/extension.js --external:vscode --platform=node --target=node18 --sourcemap --watch",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "format": "prettier --write src/",
    "format:check": "prettier --check src/",
    "package": "vsce package",
    "publish": "vsce publish"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@types/ws": "^8.5.10",
    "@typescript-eslint/eslint-plugin": "^8.59.0",
    "@typescript-eslint/parser": "^8.59.0",
    "@vscode/vsce": "^2.25.0",
    "esbuild": "^0.25.12",
    "eslint": "^10.2.1",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.3",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "ws": "^8.16.0"
  }
}
